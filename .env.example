# Copy this to .env and fill in your local values. .env is git-ignored; never commit it.

# ICID backend base URL (no trailing slash).
# Local backend (uvicorn): http://127.0.0.1:8000
# Deployed backend:        https://icid-backend.vercel.app
VITE_API_URL=http://127.0.0.1:8000
